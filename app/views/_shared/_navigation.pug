mixin navigation
    
    nav.navbar.navbar-inverse
        .container-fluid
            .navbar-header
                button(type="button" class="navbar-toggle" data-toggle="collapse" data-target="#myNavbar")
                    span(class="icon-bar")
                    span(class="icon-bar")
                    span(class="icon-bar")
                a.navbar-brand(href="/" id="homeBtn")
                    | Почивка мечта
            div.collapse.navbar-collapse
                ul.nav.navbar-nav
                    li
                        a(href="/hotels/all") Хотели
                    li
                        a(href="/regions/all") Региони
                    if user && user.role !== 'admin'
                        li
                            a(href="/orders") Резервирай
                    if user && user.role === 'admin'
                        li
                            a(href="/admin/panel") Админ Панел
                ul.nav.navbar-nav.navbar-right
                    if !user
                        li
                            a(href="/login" id="loginBtn")
                                | Влез
                        li
                            a(href="/register" id="registerBtn") 
                                | Регистрирай се
                    else
                        li
                            a(href="/profile" id="profileBtn")
                                | Здравей, #{user.username}
                        li
                            a(href="/logout" id="registerBtn")
                                | Излез