const index = require('./indexPage-controller');
const hotels = require('./hotels-controller');
const regions = require('./regions-controller');
const users = require('./users-controller');

module.exports = {
    index,
    hotels,
    regions,
    users,
};
